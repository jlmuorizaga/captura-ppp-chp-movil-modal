import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'captura-ppp-chp-movil-modal',
  webDir: 'www'
};

export default config;
